# PRE-REGISTERED before the run — the fingerprint package (packages/GSE283483-multiome-gex.fingerprint.tar.gz)
# awaits upload to the HF benchmark repo by an account with write access; until then `eval run` on this
# case SKIPs (package unreachable), and this file records the prediction the run will be tested against.
#
# library.chemistry is what seqforge resolved from the Multiome GEX arm's bytes on arc at the `predicts`
# versions (SRR34104052, an over-sequenced 28bp CB+UMI R1 that resolves to the ARC-whitelist child of
# 3' v3). experiment.organism is transcribed from the BioSample record (records.json — Mus musculus).
outcome: decide
description: >-
  Benchmark: GSE283483 10x Multiome GEX / snRNA arm (SRR34104052) resolved from its fingerprint package;
  the single-cell RNA half of the Multiome driving dataset, told from plain 3' v3 by the ARC whitelist.
predicts:
  seqforge: 2026.7.1
  kb: 2026.7.7
  workflow: 2026.7.14
fields:
  library.chemistry: 10x-multiome-gex
  experiment.organism: 10090
