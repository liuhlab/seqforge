# GSE283483 bulk RNA arm (SRR31555437), run from its byte-light fingerprint package on the public HF
# repo. Pulled anonymously (pooch-cached, no token) only by the opt-in benchmark job; unreachable => skip.
generate:
  kind: fingerprint
  hf: packages/GSE283483-bulk.fingerprint.tar.gz
