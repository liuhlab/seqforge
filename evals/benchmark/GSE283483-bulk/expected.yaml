# PRE-REGISTERED before the run — the fingerprint package (packages/GSE283483-bulk.fingerprint.tar.gz)
# awaits upload to the HF benchmark repo by an account with write access; until then `eval run` on this
# case SKIPs (package unreachable), and this file records the prediction the run will be tested against.
#
# library.chemistry is what seqforge resolved from the bulk arm's bytes on arc at the `predicts` versions
# (SRR31555437, RNA-Seq paired-end, PRJNA1192296). experiment.organism is transcribed from the archive's
# own BioSample record (records.json beside this file — Mus musculus). Both are checkable claims.
outcome: decide
description: >-
  Benchmark: GSE283483 bulk RNA arm (SRR31555437) resolved from its fingerprint package; the paired-end
  bulk half of the Multiome + bulk driving dataset for #82's modality-general architecture.
predicts:
  seqforge: 2026.7.1
  kb: 2026.7.7
  workflow: 2026.7.14
fields:
  library.chemistry: bulk-rnaseq-pe
  experiment.organism: 10090
