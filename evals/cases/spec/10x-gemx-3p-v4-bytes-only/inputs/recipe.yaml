# GEM-X 3' v4: byte-identical geometry to 3' v3 and v3.1, separable ONLY by its 3M-3pgex-may-2023
# whitelist. Synthetic onlists register the exact pool the reads are drawn from (and the v3
# 3M-february-2018 exclude probe as a disjoint pool), so the v4-vs-v3 divergence is settled by the
# whitelist at rung 3 — which is the whole reason this spec exists.
generate:
  kind: spec
  spec: 10x-gemx-3p-v4
  n: 3000
  seed: 0
  onlists: synthetic
