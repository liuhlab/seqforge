# 11. The instructable surface is closed: an aligner param has three owners, and an affordable ambiguity is dissolved rather than asked

**Status.** Absorbs ADR-0012 and ADR-0022 — the third owner, and dissolving rather than asking.

One key space for both parsing and counting would let an instruction contradict the bytes, and both
answers are wrong — honouring it corrupts the matrix, refusing it makes the surface a lie — so parse
keys and count keys are disjoint. A third owner follows from what a value varies with rather than
what it is for: a flag varying with neither chemistry nor intent is a module literal, since a recipe
key whose only other reachable state is wrong is a trap with a UI and a KB key would oblige eleven
specs to declare one identical fact. Counting living in the recipe then lets an ambiguity one pass
can afford every answer to dissolve rather than escalate — `soloFeatures` defaults to all five —
because an exit code firing when it need not teaches callers to route around the ones that must.
